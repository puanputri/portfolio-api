apiVersion: v1
kind: Service
metadata:
  name: portfolio-api
  namespace: default
  labels:
    app: portfolio-api
spec:
  type: ClusterIP
  selector:
    app: portfolio-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
